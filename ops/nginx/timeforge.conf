# Synology Web Station server include. Keep HTML revalidated after deployments.
location ^~ /Projects/TimeForge/ {
    root /var/services/web;
    add_header Cache-Control "no-cache, must-revalidate" always;
    try_files $uri /Projects/TimeForge/index.html;
}
